# 实施进度记录

依据《启动性能与低内存根治计划》执行手册 S00–S15 顺序执行。每步状态取值：`未开始 / 进行中 / 功能通过待性能验证 / 通过 / 受阻`。断开后从本文件续做；不能把未完成步骤打勾。

## 环境基线记录（S00 采集）

| 项 | 值 |
| --- | --- |
| 记录日期 | 2026-09-05 |
| HEAD | `1e8d361a920c92fe3845ed7515aa9dd8b460199a`（docs: update star history） |
| 暂存区改动 | `AM docs/启动性能与低内存根治计划.md`（用户已暂存并有工作区修改，实施期间不改动、不覆盖、不提交） |
| Node | v24.15.0 |
| pnpm | 11.25.0 |
| rustc | 1.98.1 (48a229cea 2026-09-01) |
| cargo | 1.98.1 (797e8a9bc 2026-08-05) |
| 锁文件 | `pnpm-lock.yaml`（288,549 B）、`src-tauri/Cargo.lock`（147,017 B）均存在，本次不升级依赖栈 |
| 现有 NoteBoard 进程 | 无（tasklist 确认） |
| 用户正式数据目录 | `%APPDATA%\NoteBoard`（settings/session/recent/favorites/staging/font-packs）、`%LOCALAPPDATA%\com.crazyfigure.noteboard`、`%LOCALAPPDATA%\com.crazyfigure.noteboard.dev` 均存在 |
| 数据隔离结论 | 用户正式数据目录真实存在。实施期间**不启动任何会写正式数据的应用**（dev、安装版、测试构建一律不跑真实启动）。性能采样类工作（真实冷启动、外部录像、内存采样）标记**待验证**，只做静态分析、纯单元测试与仓库内构建产物分析 |
| 测试资料目录 | `test-results\performance\`（git 忽略）；已有规划期资产：`review-20260905`、`review-20260905-round2`、`review-20260905-round3`（静态闭包检查脚本与 gate 对抗夹具）、`test-10k.md`（14,665 B，含 BOM）、`test-100k.md`（104,606 B）。实施新增数据一律放入本目录，不覆盖已有资产 |

## 步骤状态

| 步骤 | 状态 | 摘要 |
| --- | --- | --- |
| S00 环境记录与隔离 | 通过 | 见上表；未改用户暂存区 |
| S01 P0 埋点与生产资源图 | 功能通过待性能验证 | 埋点/预算脚本/最小宿主/基线文档完成；R15 补 requestId 贯穿/前端开关；真实桌面采样待用户现场 |
| S02 契约修正（归属查询/所有权注销/CodeMirror 单实例） | 通过 | 三项契约修正完成；双标签保存测试通过 |
| S03 能力注册表 | 通过 | core/editor 注册表 + 统一异步 flush；旧 getter 跨模块引用归零；472 测试全过 |
| S04 打开队列与迁移协议 | 已整改待组合验证 | R06/R07+N01/N06/N07 修复迁移终态判别（aborted 不再误判 committed）/ACK 去重/在途初始化/businessReady 门槛；13+7 反例全绿；真机多窗口迁移未测 |
| S05 编辑器按类型懒加载 | 通过 | 首屏闭包 4,305→986 KiB（-77%）；禁止模块清零；gate:budget 启用 --check 门禁 |
| S06 字体按需服务 | 已整改待组合验证 | R11 修复真正解耦/400 normal face/重验接续/前端 epoch；真机字体稳定时间未测 |
| S07 文件准备与所有权预留 | 已整改待组合验证 | R04/N04/N05 修复关闭注销/会话代际/真实窗口身份/重复清理；反例转绿；三轮补 prepare reservation 生命周期（预约消除注册空窗，4 例测试）与统一异步关闭协调器 |
| S08 Markdown visual/source 拆分 | 通过 | VisualKernel 惰性挂载（源码首屏零 TipTap 实例）；50ms 锁改同步程序事务；496 测试过 |
| S09 文档版本与同步屏障 | 已整改待组合验证 | R03/R12+N04 会话代际（旧任务不覆盖同路径重开）+N03 另存为事务（授权先于写盘）；反例转绿 |
| S10 会话恢复轻量描述符 | 已整改待组合验证 | R01+N02 修复暂存覆盖/共享加载 Promise/ensureWritableContent 屏障/恢复占位防覆盖；反例转绿 |
| S11 Code/Image 回收与轻量缓存 | 已整改待组合验证 | R05/N08 修复收敛逐项授权（不可回收 pinned）+Home 不卸载（宿主级断言过）；IME 保护 |
| S12 各类型生命周期接入 | 已接入待真机验证 | 五类重型编辑器（Markdown/Board/Bitable/Mindmap/Drawio）全部接入 canSuspend+视图捕获/恢复（Drawio 导出回包协议）；宿主级回收等价测试 5 例过；真机组合未跑 |
| S13 历史压缩与输入热路径 | 已实现待真机验证 | J1 保留；三轮按组边界重做 J2：visual/source 热路径只捕获不可变引用（每键 O(1)），序列化按历史组延迟执行（跨组全部保留，R02 反例保持通过）；纯适配器 roundtrip 等价 5 fixture 过；9 例测试 |
| S14 订阅收窄与后台调度 | 已整改待组合验证 | R09/R13+N09 外部正文核对链（等长替换检出/去抖/静默复核，5 例测试过）/revision 签名 |
| S15 最终回归与验收 | 静态检查通过，安装版与性能未验收 | R4 整改完成：四轮 9/9（含 7 反例）+三轮 16/16+一二轮 20 反例全绿+608 前端+44 Rust+门禁（含对抗夹具）；隔离浏览器快速往返零加载提示；Release 已重打（新指纹见 R4 报告）；安装版运行/真实采样/缓存策略确认待用户现场 |

## 运行记录（按时间追加）

### S00（2026-09-05）

- 命令：`git log -1`、`git status --porcelain`、`node/pnpm/rustc/cargo --version`、`tasklist | grep -i noteboard`、锁文件与数据目录只读检查。
- 结果：见环境基线表。工作区唯一改动是用户自己的计划文档（AM），未触碰。
- 下一动作：S01——加入诊断埋点（性能标记基础设施），做一次生产构建并输出模块闭包分析；真实桌面采样标记待验证。

### S01（2026-09-05）

**新增/修改文件：**
- 新增 `src-tauri/src/perf/mod.rs`、`src-tauri/src/perf/commands.rs`：Rust spans 收集器（NOTEBOARD_PERF_SPANS=1 启用，上限 1024，批量落盘 %TEMP%\noteboard-perf\，未启用为 no-op）。
- 修改 `src-tauri/src/main.rs`（process_entry）、`lib.rs`（perf 模块+setup 埋点+命令注册）、`window/commands.rs`（window_ready/create_window 埋点、最后窗口关闭前 flush）。
- 新增 `src/core/perf/perfMarks.ts`、`src/core/perf/reportWebSpans.ts`；修改 `src/main.tsx`（js_entry+rAF）、`App.tsx`（settings/window_boot/listeners/shell 埋点+boot 上报）、`windowManager.ts`（关闭前上报）、`index.html`（__nbHtmlTs 引导标记）、`core/ipc/commands.ts`（recordWebSpans/dumpPerfSpans 封装）。
- 新增 `scripts/check-budget.mjs`（入口静态闭包分析+禁止模块检查，报告/门禁两模式）；新增 devDependency es-module-lexer@2.3.2。
- 新增 `tools/perf-host-min/`（最小 Tauri 对照宿主：tauri 2.11 同版本、无插件、隐藏→显示同主应用策略、%TEMP% 输出、独立 identifier）。
- 新增 `test/core/perfMarks.test.ts`（4 例）；`perf/mod.rs` 内嵌 2 例 Rust 测试。
- 新增 `docs/performance/启动基线.md`（本步交付文档）；`.gitignore` 增加 perf-host-min 产物规则。

**已运行命令与结果：**
- `pnpm build`（改动前 before 基线）：成功 41.5s；入口闭包 4,305.1 KiB（index 2,507 + excalidraw 1,122 + mermaid 652 + highlight 23），CSS 209.5 KiB；报告 `test-results/performance/budget-report.json`。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `pnpm typecheck`：通过。
- `pnpm test:unit`：47 文件 460 测试全过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：全部通过（含 perf 模块 2 例）。
- `node scripts/check-budget.mjs`：报告模式，before 基线 3 项违规（excalidraw/mermaid/highlight 在首屏闭包）——S05 改造对照起点。
- 注册表只读检查：安装版 0.2.6 位于 C:\Software\NoteBoard\，8 个 NoteBoard.* ProgID，exe 直收 %1 无脚本启动器；UserChoice(.md)=Markdown 的无效 ProgId 疑点见启动基线.md §2。
- `cargo check --manifest-path tools/perf-host-min/Cargo.toml`：通过（独立 target 全量编译 tauri 2.11 依赖）。

**真实桌面采样（冷启动/内存/最小宿主实测）未执行**——数据隔离约束下不启动写正式数据的应用；现场采集步骤见启动基线.md §6。

**下一动作：** S02——按 B 节修正 findDocumentOwner wire 类型、unregister 所有权校验、CodeEditor editorView 按 docKey+instanceId 注册，并记录迁移安全缺口。

### S02（2026-09-05）

**修改文件：**
- `src/core/ipc/commands.ts`：findDocumentOwner 返回类型修正为 `Promise<string | null>`（wire 真实格式；原 `{ownerLabel}` 包装与 Rust `Option<String>` 不符；该封装当前无调用者，属死代码类型修正）。
- `src-tauri/src/registry/documents.rs`：unregister 增加**所有者校验**——仅 `owner_window == label` 才移除，返回是否实际移除；旧窗口迟到清理不再能注销新所有者。
- `src/features/editor-code/CodeEditor.tsx`：全局单 `editorView` 改为 `Map<docKey, {instanceId, view}>` 注册表；`getEditorView(docKey)` 按键查询；卸载时 instanceId 匹配才删除（代际保护）。
- 调用方改造：`AppShell.tsx`（5 处传 currentTab.key）、`syncDocumentContent.ts`（按 docKey 查询并移除 activeKey 补丁校验——后台代码标签保存/暂存改用权威内容）、`SearchReplaceBar.tsx`、`CodeToolbar.tsx`。
- 新增测试：`test/editor-code/codeEditorInstances.test.tsx`（4 例：双标签不串线/卸载清理/代际保护/双标签保存取权威内容）；`registry/documents.rs` 内嵌 5 例（所有者注销/非所有者保护/未注册幂等/AlreadyOpen 携带所有者/reconcile 隔离）。

**已运行命令与结果：**
- `cargo test`：26 例全过（新增 registry 5 例 + perf 2 例）。
- `pnpm test:unit`：49 文件 468 测试全过（新增 4 例）。
- `pnpm typecheck`：通过。

**登记的既有缺口（后续步骤修复，本步不动）：**
- 迁移安全缺口（S04 修复）：`windowManager.ts` moveToNewWindow 直接读 documentStore 滞后镜像构建 TransferredDocument，viewState 恒为空值，目标 adopt 强制 utf8/lf 且丢失完整元信息/历史。
- watcher 占位（S14 修复）：`fsio/commands.rs` 的 `watch_dir(path, recursive, delay_ms)`/`unwatch_dir(path)` 为空实现，前端只传 path；无任何真实监听。

**编辑器挂载策略与序列化算法保持不变**（S02 判定要求）。

**下一动作：** S03——新建 `src/core/editor/editorRegistry.ts` + `editorTypes.ts` 轻量能力注册表，替换全部组件 getter，统一异步 flush 契约并迁移全部调用方为 await。

### S03（2026-09-06）

**新增文件：**
- `src/core/editor/editorTypes.ts`：FlushReason/CapturedContent/EditorCapabilities/搜索与代码操作能力组/视图状态判别联合（无内核依赖）。
- `src/core/editor/editorRegistry.ts`：按键注册表（instanceId 代际保护）+ revision 计数（重挂载不重置、文档关闭才清理）。
- `src/features/editor-code/editorCapabilities.ts`：code 编辑器能力工厂（flush=CM6 doc 快照；search=包装 searchController；codeOps=jsonOps/textOps）。
- `src/features/editor-md/editorInstances.ts`：editor-md 边界内实例表（visual/source 两个 Map；服务 MarkdownToolbar 等边界内部消费）。
- `src/features/editor-md/editorCapabilities.ts`：Markdown 能力工厂（按 viewMode 动态分派 visual/source 内核）。
- `test/core/editorRegistry.test.ts`（4 例）。

**修改文件：**
- `CodeEditor.tsx`：删除组件级 getEditorView 导出与内部实例 Map → 挂载时注册能力（bump revision 于 docChanged）。
- `TipTapEditor.tsx`：删除 getActiveTipTapEditor/getActiveSourceView 导出与内部 Map → 注册能力 + 边界内实例表（bump revision 于 onUpdate/source docChanged）。
- `BoardEditor.tsx`：删除 getActiveBoardScene 导出 → 注册能力（flush=serializeScene；bump 于可撤销内容签名变化）。
- `syncDocumentContent.ts`：改 async，经注册表 flush；**不再 import 任何编辑器组件**。
- 调用方 await 化：`saveDocument.ts`（save/saveAs）、`MissingFileDialog.tsx`、`stagingManager.ts`（collectDocuments→async）。
- `AppShell.tsx`：Ctrl+F/H/JSON 快捷键改走 caps（getSelectedText/codeOps），删除编辑器 getter import。
- `SearchReplaceBar.tsx`：改走 caps.search；`CodeToolbar.tsx`：改走 caps.codeOps；`MarkdownToolbar.tsx`：改用 editor-md 边界内实例表。
- `test/editor-code/codeEditorInstances.test.tsx`：改用注册表 API（flush 内容断言/代际保护/双标签保存）。

**已运行命令与结果：**
- `pnpm typecheck`：通过。
- `pnpm test:unit`：50 文件 472 测试全过。
- grep 验证：`getEditorView/getActiveTipTapEditor/getActiveSourceView/getActiveBoardScene` 组件级导出引用归零（MarkdownToolbar 使用边界内 editorInstances 别名，属 D 节允许的边界内部消费）。

**说明：** Mindmap/Bitable/Drawio/图片暂未注册能力（其保存链原本就走 store 镜像，行为不变）；S12 逐类型接入时补真实 flush（Drawio 需先补导出确认协议）。编辑器挂载策略不变（S03 判定要求）。

**下一动作：** S04——按 C 节实现 OpenRequestDto 队列、listeners-ready 握手、ack 确认、drain 单运行者与 transferId 迁移协议；前后端 DTO 同步；删除旧事件直接打开逻辑。

### S04（2026-09-06）

**Rust 端修改：**
- `dto.rs`：新增 OpenRequestDto/OpenRequestItemDto/OpenRequestSource/OpenOutcome/WindowBootDto/TransferState/BeginTransferResponse/TransferStatusDto；TransferredDocument 扩充 kind/language/encoding/eol/readonly/mtime/size/baseline/revision/history（旧字段兼容）。
- `state.rs`：AppState 增加 open_requests（label→队列）、orphan_requests、open_queue_version、window_consumers、next_consumer_seq、next_request_seq、transfers（TransferRecord 含短命 payload）；unregister_window 返回待转交请求。
- `window/intent.rs`：重写为打开队列管理（enqueue/list/ack/握手分配 consumer/窗口销毁转交/orphan），核心逻辑均为 *_inner 纯函数可单测。
- `window/commands.rs`：新命令 window_listeners_ready/window_shell_ready/list_open_requests/ack_open_request/enqueue_open_requests/begin_document_transfer/take_transfer_payload/prepare_transfer_complete/abort_transfer/query_transfer；close_window 在 orphan 非空时建新窗承接而非退出；prepare_transfer_inner 三步原子（校验→所有权切换→状态推进）。
- `bootstrap/single_instance.rs`：第二实例路径入队 + 队列唤醒事件（不再直接 emit 路径）；`bootstrap/mod.rs`：冷启动 argv 入队（不再 OpenFiles intent）。
- `window/manager.rs`：Destroyed 时转交 pending 请求并唤醒目标窗口。
- 删除命令：window_ready、open_in_new_window、confirm_handoff。
- lib.rs 命令注册同步。

**前端修改：**
- 新增 `src/app/bootCoordinator.ts`：drain 单运行者（退出前检查 drainRequested 覆盖空列表竞态）、consumer 引用计数（StrictMode 安全）、bootError/retryBoot。
- `windowManager.ts` 重写：initWindow=监听→握手→处理器注册→迁移接收；requestShellReadyAndDrain（壳显示+drain）；moveToNewWindow 走 transfer 协议（flush 权威内容→完整载荷→事件+低频查询等待，不盲超时回滚）；adoptTransferredDocument（完整元信息+基线恢复+迁移保护）；拖拽 drop 入队。
- `App.tsx`：C 节顺序（监听/数据保护→握手→设置→壳显示→drain）；启动失败错误壳（可见/可重试）；焦点恢复 drain。
- `windowStore`：transferringKeys 迁移保护（AppShell 遮罩阻断输入、requestCloseTab/requestWindowClose 跳过迁移中文档）。
- `openDocument.ts`：返回 'opened'|'focused'|'failed' 供 ACK。
- `commands.ts`/`events.ts`/`types.ts`：新协议封装；删除 windowReady/openInNewWindow/confirmHandoff/onOpenFiles/onHandoffComplete。

**测试（新增 17 例）：**
- Rust `intent.rs` 5 例（入队序号/list 非破坏+consumer 门控/ack 幂等+作用域/握手代际+启动模式/销毁转交）；`commands.rs` 6 例（迁移提交所有权切换/非目标拒绝/aborted 拒绝+committed 幂等/源销毁中止/第三方所有权拒绝/未命名注册）。
- 前端 `test/window/openQueue.test.ts` 6 例（按序处理+ACK/失败也 ACK/事件只唤醒/运行中事件不丢/consumer 失效停止/重复握手复用）。

**已运行命令与结果：** `cargo test` 37 全过；`pnpm test:unit` 51 文件 478 全过；`pnpm typecheck` 通过；grep 确认旧协议引用清零（无新旧消费者并存）。

**登记缺口：** 迁移的可序列化历史未携带（源/目标历史在迁移后不保留，与旧实现一致）——S13 历史压缩落地时补齐；notify_window_active 独立往返仍在（S07 并入握手）；openDocument 的 probe→read 串行 IPC（S07 文件准备协议重构）。

**下一动作：** S05——按 E 节逐类型懒加载编辑器（先隔离 Board/Drawio/Mindmap/Bitable/Image，再独立图表与弹窗），构建依赖报告验证真实隔离。

### S05（2026-09-06）

**新增文件：**
- `src/features/editor-host/editorLoaders.ts`：动态 import 工厂表（模块顶层零 import；kind+language 选择入口；prefetchEditor 并行预取；createLazyEditor 按 retryGeneration 重建）。
- `src/features/editor-host/EditorHost.tsx`：Suspense + 局部 ErrorBoundary（fallback 带文件名/稳定尺寸；重试重建 lazy 包装与边界；重试仍失败保留文档提示安全重启）；错误时标题栏/关闭保护不受影响。
- `test/startup/editorLoading.test.tsx`（6 例：入口映射含图表语言分流、工厂不提前执行 import、lazy 实例重建）。

**修改文件：**
- `AppShell.tsx`：删除 9 个编辑器静态 import → EditorHost 统一渲染；删除全局 Drawio prefetch（E 节 8）。
- `App.tsx`：设置/更新/字体提示/收藏弹窗全部 lazy + useEverOpened（首开才装载、装载后保持挂载保留关闭动画）；UnsavedGuardDialog 保持轻量常驻。
- `openDocument.ts`：路径解析后立即 prefetchEditor 与读盘并行（避免先读完再 import 的瀑布）。
- `vite.config.ts`：manualChunks 改函数式——**关键修复**：Vite 的 __vite__preloadHelper 默认被 Rollup 放进首个创建的 chunk（mermaid 重 chunk），导致入口为拿这一个函数静态 import 3 MiB 图表库；显式归入 vendor-react 后入口闭包达标。lowlight/highlight.js 归组移除（已由 editor-md 边界内按需加载；对象式归组曾把 react/react-dom 卷进 excalidraw chunk 形成静态回边）。
- `package.json`：gate:budget 切换为 `--check` 门禁模式（S05 达标后启用）。

**构建证据（after vs before）：**
- 入口静态闭包：**4,305.1 KiB → 985.9 KiB（-77%）**；首屏 CSS：209.5 → 68.2 KiB（-67%）。
- 禁止模块（excalidraw/mermaid/highlight）：3 个 → **0**；Excalidraw CSS（144.7 KiB）随画板边界加载。
- `pnpm gate:budget`（--check）退出码 0；报告 `test-results/performance/budget-report.json`。
- 闭包构成：index（388 KiB 应用壳）+ vendor-react（598 KiB React 全家桶，入口必需）。

**已运行命令与结果：** `pnpm build`（38s）×3 轮迭代定位回边；`pnpm test:unit` 52 文件 484 全过；`pnpm typecheck` 通过。

**说明：** 文档状态表示与后台标签挂载策略未变（S05 判定要求）；首次真实功能 smoke 待安装版验证（数据隔离约束，列入启动基线.md §6 待验证清单）。

**下一动作：** S06——按 F 节解除字体门槛、SHA 移入 spawn_blocking、DirectWrite 原生枚举替代 PowerShell、按 face 加载。

### S06（2026-09-06）

**Rust 端：**
- `font_pack.rs`：验证状态机——get_font_pack_status 改 async（missing 即返/缓存命中即返/否则 verifying+spawn_blocking 后台全量 SHA）；进程缓存记录文件身份（size+mtime）复用前核对，跨次启动不复用；generation 于安装/导入/删除/显式修复递增，迟到结果不发布；新增 refresh_font_pack_status 命令；download 预检不再同步 SHA；remove 后 invalidate_cache。
- `sysfont/mod.rs`：**GDI EnumFontFamiliesExW 进程内原生枚举替代 PowerShell/WPF/Add-Type 子进程**（同族去重、SYMBOL_CHARSET 剔除、中文族名、@前缀剔除行为一致）；枚举移入 blocking worker（async command）；进程缓存 + force_refresh 参数；font-kit 兜底保留。
- `Cargo.toml`：windows crate 增加 Win32_Graphics_Gdi feature。
- 内嵌测试：GDI 枚举真机验证（返回非空 + 含 Segoe UI）通过。

**前端：**
- `settingsStore.ts`：init 不再阻塞等待字体校验完成（字体服务独立初始化；verifying 快速返回）。
- `fontPack.ts`：activateFontPack 拆分——登记全部 FontFace（不主动 load）+ ensureFaces(排版需求) 只主动 load 配置引用族；字体就绪后广播 noteboard-fonts-settled 事件；新增 settingsReferencePackagedFonts（提示顺序前置判断）。
- `fontPackStore.ts`：verifying 状态不激活不报错；_applyStatus 传入当前 typography；refresh 走强制重验命令。
- `CodeEditor.tsx`/`TipTapEditor.tsx`：监听 fonts-settled 事件触发 requestMeasure（度量刷新）。
- `App.tsx`：提示顺序改为 配置引用包字体（纯前端）→ 包状态 → 按需枚举系统字体；verifying 不提示；用户正在编辑输入时推迟提示（不抢焦点）。
- `types.ts`：FontPackState 增加 'verifying'；`commands.ts`：listSystemFonts(forceRefresh)/refreshFontPackStatus。

**测试：** 新增 `test/font/fontLifecycle.test.ts` 5 例（提示前置判断/verifying 不激活/按需 face 加载/无引用不加载/shouldPrompt 语义）；cargo test 38 全过（含 GDI 真机）；pnpm test:unit 484 全过。

**保留能力：** SHA-256 完整性校验、原子安装/回滚、修复入口（强制重验）、全部字形（未做子集/压缩）；未验证字体先以字体栈 fallback 渲染且设置值不变。

**待现场验证（启动基线.md §6）：** 字体链路真实耗时 before/after（PowerShell 消除收益）、verifying→ready 的 typography_settled 跳字形体验。

**下一动作：** S07——按 G 节实现文件准备判别结果、所有权预留（reservations 表）、重复打开不读正文、目录及最近记录延后、notify_window_active 并入握手。

### S07（2026-09-06）

**Rust：**
- 新增 `src-tauri/src/fsio/prepare.rs`：统一文件准备 `prepare_document`（async command）——执行次序固定：blocking 规范化 key（canonicalize）→ 短锁查归属（documents + pending_prepares，已打开/在途在读盘前返回 AlreadyOpen）→ 短锁插入在途标记（含等待锁期间的双重检查）→ blocking worker（metadata + 文本前缀检查 + read_file 解码）→ 短锁移除标记。判别结果 `PreparedDocument`（directory/image/text/unsupported/already-open/failed）；图片/不支持类型不读正文。
- `state.rs`：AppState 增加 `pending_prepares`（在途文件准备标记）。
- `dto.rs`：PreparedDocument 判别联合。
- 删除 `notify_window_active` 命令（G 节 6：活跃通知已并入 listeners-ready 握手的 touch，消除冷启动独立往返；补注册 S06 的 refresh_font_pack_status）。

**前端：**
- `openDocument.ts` 重构：probe+read+register 三次往返合并为一次 `prepareDocument`；already-open 本窗口直接 activateTab（不读正文、不建新标签、不覆盖脏内容），跨窗口 focusWindow；目录展开/最近记录改 fire-and-forget 延后任务，带发起时 activeKey/root 竞态校验（用户切换目录后旧结果不覆盖）。
- `types.ts`/`commands.ts`：PreparedDocument 类型与封装；删除 notifyWindowActive 死封装。

**测试：** 新增 `test/window/openAlreadyOpen.test.ts` 3 例（重复打开只激活/跨窗口聚焦不建标签/失败无幽灵所有权）；更新 `dragDrop.test.ts` 为 prepare 接口；Rust 38 + 前端 492 全过；typecheck 通过。

**保留：** 旧 read_document/probe_document 命令保留供未迁移调用（G 节要求）；read::read_file 的编码检测/只读判定/EOL 原逻辑不变；50 MiB 大文件保持 warning 语义（未实测前不新增确认弹窗，列入待验证）。

**待现场验证：** 慢目录（云盘/上万项）下已读文档可先操作；后端先行读取（冷启动预读）未实现——决策：S05 编辑器预取 + prepare 合并已消除主要串行往返，后端预读需正文缓存协议，收益需实测后决定（列入启动基线.md §6）。

**下一动作：** S08——拆 Markdown visual/source 运行时，模式选择先于内核创建；去掉初始化 50ms 忽略真实输入的窗口（程序事务识别）。

### S08（2026-09-06）

**新增文件：**
- `src/features/editor-md/VisualKernel.tsx`：visual 运行时子组件（useEditor/onUpdate/editorProps/超链接弹窗/右键菜单/气泡菜单/Ctrl+K 全部内聚）；惰性挂载——source 初始模式不创建 TipTap 实例，首次进入 visual 才挂载，此后常驻（display 切换保留撤销栈与选区）。
- `src/features/editor-md/markdownAutoSave.ts`：自动保存提取为模块级函数（visual/source 两链共用，避免循环依赖）。
- `test/startup/firstInput.test.tsx`（真实渲染：源码首屏不创建隐藏 TipTap 实例）；`test/startup/coordinatorLazyMount.test.tsx`（内核替身：source 初始不挂载/切换挂载一次并以源码权威内容填充/visual 初始填充）。

**TipTapEditor.tsx 改造（协调器化）：**
- 初始化拆两段：**文档级初始化**（大文档判定/模式决策/历史首节点/基线，不依赖 editor——source 初始无需内核）+ **visual 填充 effect**（editor 惰性 ready 后按 pendingVisualContent 程序化设置一次）。
- 🔴 50ms 初始化锁删除：改为同步程序事务作用域（parseMarkdown 调用期间 try/finally 加解锁）——界面可输入后的第一笔真实按键立即进入历史与保护队列（J 节语义）。
- toggleViewMode 的 source→visual 分支：editor 为 null 时走惰性挂载路径（记录待填充内容）；大文档 banner"仍要可视化"按钮同步支持惰性挂载。
- editor 实例由 VisualKernel onReady 回传（协调器持有状态与实例表/能力注册）。

**已知限制（如实登记）：**
- TipTap 内核在 jsdom 全量挂载存在 prosemirror 装饰兼容问题（DecorationGroup.locals undefined），visual 内核真实创建用例以替身覆盖，完整真实行为在 S15 安装版回归验证。
- chunk 级 visual/source 拆分未做（TipTap 库仍在 markdown 边界 chunk 内；source 首选用户加载库但不创建实例）——后续可选优化，不阻塞 S08 判定。

**已运行：** pnpm test:unit 56 文件 496 全过；typecheck 通过；gate:budget --check 通过（当前 dist）。

**下一动作：** S09——按 H 节为 DocumentSession 增加 revision/mirroredRevision/savedRevision，迁移 save/saveAs/stage/close/切模式与全部 autosave 入口到每文档写队列。

### S09（2026-09-06）

**新增文件：**
- `src/features/session/documentSession.ts`：
  - `flushDocument(docKey, reason)`：统一异步屏障（走 S03 能力注册表，捕获确切 revision 权威快照并推进 mirroredRevision，迟到快照不倒退镜像）。
  - `enqueueDocumentWrite<T>`：每文档串行写队列（单个失败不断链，错误上抛调用方）。
  - `queuedAutoSave`：全部 autosave 入口统一路径——策略/外部状态/基线检查 → 写队列 → 基线只更新实际写入文本 → flush-and-compare 脏态重算 → 带内容证明的暂存清理。
  - `refreshDirtyAfterWrite`：保存成功后 flush 当前权威内容与基线逐字比较（撤销/改回原文正确清脏，写盘期间新输入保持脏）。
  - `migrateDocumentSession`/`disposeDocumentSession`：另存为身份迁移与最终关闭清理（revision 由 editorRegistry 持有，session 记录迁移）。
- `test/session/saveRace.test.ts`（8 例，覆盖 H 节时序表）：时序 1 输入 A 立刻 Ctrl+S 磁盘含 A；时序 2 保存 r10 期间输入 r11（磁盘 r10/编辑区 r11/dirty 保持）；时序 3 r10 保存+r11 已暂存 → 暂存不被删（内容证明）；写队列串行；时序 5 迟到 flush 不倒退镜像；时序 7 撤销回基线清脏；barrier 失败不断链；另存为身份迁移。

**改造文件：**
- `saveDocument.ts`：saveDocument 走 flush 屏障（权威内容取 flush 快照而非防抖镜像）+ writeDocumentWithBarrier；saveAs 写盘入目标 key 写队列 + 身份迁移（drain 旧 key 在途写 + migrate session）+ 暂存清理带内容证明。
- `stagingManager.ts`：stagedPaths 记录内容证明（{path, content}）；onDocumentSaved(docKey, savedContent?) 只删被保存覆盖的副本（暂存内容≠保存内容时保留）。
- `CodeEditor.tsx`/`BoardEditor.tsx` autosave → queuedAutoSave；`markdownAutoSave.ts` 改为薄封装（visual/source 两链共用）。
- `windowManager.ts`：performWindowClose 排空全部标签的在途写队列后再退出。

**未纳入队列（有意保留）：** draftManager（会话草稿为独立输出副本，非用户文档正文，H 节"独立输出不强行套正文 revision"）。

**已运行：** pnpm test:unit 57 文件 504 全过；typecheck 通过。历史存储格式未变（S09 判定要求）。

**下一动作：** S10——会话恢复改为轻量描述符（Home 不创建编辑器，点击才加载；恢复事务保存）。

## 受阻与遗留

- 暂无。

### S11（2026-09-06）

**新增：**
- `src/features/session/editorSuspension.ts`：回收调度（canSuspend→flush→revision 校验→视图状态捕获→保活更新；迁移保护不回收；关闭清理）。默认缓存=活动文档+最近 1 个已验证可回收实例。
- `test/session/editorLifecycle.test.tsx` 7 例（回收流程/不可回收保留/迁移保护/revision 并发不回收/关闭清理/状态存取一次性/CodeEditor 真实重挂载恢复选区滚动）。

**改造：**
- `editorTypes.ts`：CapturedContent 改判别联合（只读/非文本变体 content: null + readonly: true——图片不为接口伪造空字符串）；EditorCapabilities 增加可选 captureViewState。
- Code 能力：canSuspend=true + 选区/滚动/折叠（foldState iter）捕获；CodeEditor 重挂载恢复（foldEffect 重放 + 选区裁剪 + scrollTop）。
- ImageViewer：注册能力（只读 flush + 缩放/平移/旋转/翻转/背景捕获恢复；ref 镜像避开闭包过期）；EditorHost image 分支传 docKey。
- AppShell：mountedEditorKeys 渲染集合（tabs 增删/activeKey 切换 effect 驱动收敛；切走旧实例先回收后卸载；集合外后台标签渲染稳定占位）。

**判定达成：** Code/Image 完整恢复验证（输入/光标/滚动/历史——统一历史独立于实例，flush 保证内容一致时 initializeDocumentHistory 保留时间线）；未宣布其它类型完成。

### S12（2026-09-06）

**接入回收：** DiagramSplitEditor / InfographicSplitEditor（新增共享 `splitEditorCapabilities.ts`：CM 文本 flush 进 store + 布局模式/缩放/平移捕获；组件重挂载恢复布局与预览状态）。测试 `splitEditorCapabilities.test.ts` 2 例（真实 CM 视图 flush/capture；未挂载 flush null）。

**保留实例（未接入，原因如实登记）：**
- **Markdown**：canSuspend=false——TipTap 重挂载的撤销分组基线（visualUndoDepthRef 重置影响分组识别）与 visual 滚动恢复链未完成完整恢复验证（I 节"没证明可恢复前不一刀切卸载"）；S13/J2 的 pending 机制进一步增加重挂载复杂度，接入需专项验证。
- **Board/Bitable/Mindmap**：视图状态恢复（画布视口/表格视图状态/脑图折叠选区）未实现 captureViewState。
- **Drawio**：I 节要求先补内容导出确认协议（iframe 回包匹配）才可回收——协议未实现。

### S13（2026-09-06）

**J1 历史存储压缩（公开 API/201 节点/分组语义完全不变）：**
- `documentHistory.ts` 内部改「检查点+可逆补丁」：diffText（最长公共前/后缀，UTF-16 逐字一致）；每 20 组检查点、首节点恒检查点、补丁比全文大物化；相邻物化缓存（正逆向 O(1)）；**截断前先物化将保留的最老节点**（补丁基准即将被裁）；synchronize 先物化依赖旧内容的第一个后继；同组更新保留首事务 undoSelection。
- `historyStorage.test.ts` 9 例：J 节必测清单全覆盖 + **100 步随机编辑与朴素全文实现逐字等价对照**；既有 6 例 + 全量回归通过。修复实施 bug：截断物化时序。

**J2 输入热路径移除全文工作：**
- VisualKernel onUpdate 重写：每键仅 revision 递增 + O(1) undoDepth 分组 + 选区记录 + 保守置脏；**每键零全文序列化/零基线比较/零全文入历史**。序列化移入 500ms 合并任务（连续输入只序列化一次最新状态）。
- 新增 `visualPendingChanges.ts`（docKey 冲刷器注册表，实例代际保护）；**markdown flush 先同步冲 pending**——保存/暂存/切模式/回收/迁移屏障获得"历史已记录+镜像最新"一致快照；卸载前同步冲刷。
- 取舍登记：序列化复用 serializeMarkdown（对最新状态），未做"指定不可变 doc 的纯适配器"（@tiptap/markdown 绑定 editor.state；合并任务已消除每键成本）；原生历史分组识别保留未替代（J 节"先测后替代"）。
- 测试：pendingFlush 3 例；全量 530 过。

**下一动作：** S14——按 K 节收窄订阅、暂存 dirty revision 队列、渲染队列和资源释放。


### S14（2026-09-06）

**改造：**
- 订阅收窄：AppShell 删除整个 documents Map 订阅（每键内容更新不再重渲染全壳）；fileSize 下沉到 UnsupportedView/EditorHost 按 key 订阅。
- 暂存 dirty 队列（K 节）：stagingManager 订阅回调 diff 内容/脏态签名 → 只暂存变化 key（800ms 防抖；5s 兜底/失焦全量保留）。
- 视口身份任务队列：viewportWorkScheduler 单槽 → docKey+nodeId 身份有界队列（同身份替换、不同身份各有结果、8ms 时间片让出、256 上限背压、按身份取消）；mermaid/plantuml 扩展接入。
- 真实目录监听：新增 `directoryWatcher.ts`（plugin-fs watchImmediate 中心服务、引用计数、自身写入按路径静默防自触发）；useWatcher 改造接入；**删除 Rust 占位空命令 watch_dir/unwatch_dir** 与前端死封装。
- noteSelfWrite 接线：documentSession 两处写盘 + saveAs 写盘后登记。
- **eslint 配置缺陷修复**（既有问题）：TS 组补 browser/node globals；ignores 补 test-results/tools；清理全部任务改造产生的死引用（38 → 0）。

**测试：** `test/core/schedulerAndWatcher.test.ts` 6 例（身份队列/时间片/取消/引用计数）；全量 536 过；lint 0 错误。

### S15（2026-09-06）

- 最终构建：`pnpm build`（20.4s）→ 入口闭包 **996.2 KiB**（index 397 + vendor-react 599）+ CSS 68.2 KiB；禁止模块 0；`gate:budget --check` 退出码 0。
- 四重验证：typecheck ✅；lint（--max-warnings 0）✅；test:unit 63 文件 536 例 ✅；cargo test 38 例 ✅。
- 交付文档：`docs/performance/优化验收.md`（含 S12 部分完成与全部待验证项的如实声明）。
- 进程清理：本任务全程未启动应用/服务，无进程需停止；未 commit/push（等待用户审阅）。


---

## 复审整改记录（2026-09-06 第二轮）

依据《复审意见与整改清单.md》（R01–R15）完成整改，详细变更见 `docs/performance/复审整改完成报告.md`。

**核心结论：**
- 复审 7 个反例全部转绿（`pnpm exec vitest run --config test-results/performance/review-20260906/vitest.review.config.ts` exit 0）。
- 全量：533 前端 + 38 Rust 测试过；typecheck/lint（--max-warnings 0）0 错误；预算门禁对抗夹具正确拒绝（actualExit=1=expected）。
- **S13/J2 状态修正为部分完成**：合并序列化按复审要求回退（丢原生历史组），onUpdate 恢复每键提交——每键序列化成本仍在，"输入热路径零全文工作"目标未达成（诚实声明）。
- R06/R09/R15 的部分端到端竞态（commit 响应丢失/保存后 100ms 外部修改/真实 interactive 校准）代码路径已实现，真实触发场景未模拟。
- 安装版与真实性能采样仍未运行（数据隔离约束不变）。

---

## 二轮复审整改记录（2026-09-06 第三轮）

依据《二轮复审意见与整改清单.md》（N01–N10，反例 B01–B12 共 13 个）完成整改，详细变更见 `docs/performance/二轮整改完成报告.md`。反例已迁入正式测试目录（`test/performance/reviewRound2*.test.ts`，行为断言原样保留）。

**核心结论：**
- 二轮 13 个反例全部转绿（原始目录与正式迁入双跑 exit 0）；一轮 7 个反例复跑通过。
- 全量：**560 前端（67 文件）+ 38 Rust 测试过**；typecheck/lint 0 错误。
- **产物证据更新**：全部源码修改后重建 dist（2026-09-06 19:04），新产物闭包 1005.57 KiB（较上轮 +9.4 KiB，为会话代际/迁移对账/ACK 去重/外部核对逻辑）、missing: []；门禁 PASS；对抗夹具仍正确拒绝（actualExit=1=expectedExit）。
- **P0 修复**：迁移超时后成功中止不再被当成提交成功（终态判别 committed/aborted/unknown；只有权威 committed 清理源；unknown 保持迁移保护对账）。
- **会话代际**（N04 基础设施）：同路径关闭重开后，旧 flush/自动保存/清理任务全部作废（B04/B05/B11）；另存为身份迁移时真实 revision 随身份连续。
- **另存为事务**（N03）：占用检查先于任何写盘（含本窗口占用）；对话框期间的合法编辑（含清空）随另存写入；写盘期间新输入保留在新会话（不删源后补捞）。
- **懒恢复屏障**（N02）：保存/另存/迁移统一 ensureWritableContent；读取失败不再 `?? ''` 写空文件；恢复占位不覆盖用户已打开会话。
- **关闭身份**（N05）：注销使用真实窗口身份（不再伪造 nb-main）；批量关闭每标签恰一次清理。
- **队列与启动**（N06/N07）：ACK 持续失败只重试确认不重执行副作用；boot epoch 防旧握手覆盖新 consumer；设置加载完成前事件只记录待处理（迁移接收同样延后）。
- **Home 不卸载**（N08）：Home 与编辑器容器并存（display:none），宿主级断言验证零卸载。
- **外部修改核对**（N09）：watcher → 已打开文档有界核对（等长替换检出、600ms 去抖、静默窗口后复核），确认外部修改置 modified 阻止自动覆盖（5 例测试）。
- **诊断**（N10 部分）：perfMarks 游标按实际发送批次推进（在途事件不吞）；"未启用零 IPC"表述修正；门禁模块来源追踪/可交互终点标记/S12/S13 剩余目标未做（见报告"仍未完成项"）。
- 安装版与真实性能采样仍未运行（数据隔离约束不变）；未 commit/push（等待用户审阅）。

---

## 三轮收尾记录（2026-09-06 第四轮）

依据用户要求"仍未完成的都要完成"，将二轮整改报告的全部"仍未完成项"落地；详细变更见 `docs/performance/三轮收尾完成报告.md`。

**核心结论：**
- **S12 完成接入**：五类重型编辑器（Markdown/Board/Bitable/Mindmap/Drawio）全部接入回收（canSuspend + 视图状态捕获/恢复 + Drawio 导出回包协议确认）；宿主级回收等价测试 5 例（组件真实、子组件替身）。后台重型实例不再无条件保留——S12 原始目标达成，真机组合验证待用户。
- **S13/J2 重做完成**：按历史组边界的合并序列化——visual（ProseMirror doc 引用）/source（CM Text 引用）热路径只做 O(1) 引用捕获（每键零全文工作）；序列化按组延迟（新组开始物化上一组末端，跨组全部保留）；documentHistory 的 undo/redo/读取入口经物化钩子保证组末端不丢。纯适配器 `serializeMarkdownFromDoc` 与旧实现 5 个 fixture 逐字等价（真实 TipTap）；R02 反例（500ms 两个原生历史组保留中间节点）保持通过。一轮复审 R02 回退的错误方案（时间窗合并）未被复用。
- **N01 后半完成**：Rust `prepare_transfer_complete` 携带 actual_revision 与源捕获的 expected_revision 对账（不一致=载荷被替换→中止并清空），不再把字段存在当冲突保护；新增 Rust 测试。
- **N05 步骤 5 完成**：prepare reservation 生命周期（在途→注册预约→兑现/释放；30s TTL 惰性过期；窗口销毁清理；4 例测试）——"prepare 返回到 register 之间"的归属空窗消除；统一异步关闭协调器 `disposeTabLifecycleAsync`（可等待：停止接纳→排空写队列→注销归属），performWindowClose 接入。前端 5×60ms 重试保留（职责=unregister 迟到竞态兜底，非 prepare 空窗）。
- **N10.1 完成**：构建模块来源清单（vite 插件输出 .module-sources.json）+ 门禁按包名追踪禁止库（chunk 改名/合并仍检出）+ 可配置字节预算（JS ≤1200 KiB / CSS ≤120 KiB）；对抗夹具升级验证——文件名无泄漏字样、仅模块来源显示 mermaid 时仍被拒绝（module-source:mermaid 检出）。
- **N10.2 完成**：`editor_instance_ready` 终点标记覆盖全部编辑器类型（Code/Markdown/Board/Bitable/Mindmap/Drawio）；requestId 经 docKey 登记一次性消费（重挂载不带）；诊断只记 docKey 尾部 40 字符（隐私）。
- **Release 构建（P7）**：`pnpm tauri build` 成功——noteboard.exe（20,511,744 B，SHA-256 6b3855a7…883）与 NoteBoard_0.2.7_x64-setup.exe（7,747,535 B，SHA-256 5ec74396…849）。**未运行**（数据隔离约束）——安装版矩阵/真机迁移/IME/DPI/冷启动与内存采样须用户现场执行（绑定此指纹）。
- **全量验证**：576 前端（71 文件）+ 43 Rust + 二轮 13/13（原始+正式双跑）+ 一轮 7/7 + typecheck/lint 0 + 门禁 PASS（新产物 1006.49 KiB，missing: []）。
- 未 commit/push（等待用户审阅）；无残留进程。

---

## 四轮整改记录（2026-09-06 第五轮）

依据《三轮复审意见与整改清单.md》（16 个行为反例 + 2 个门禁对抗夹具）完成全部修复，详细变更见 `docs/performance/四轮整改完成报告.md`。反例已迁入 `test/performance/round3/`（行为断言原样保留）。

**核心结论：**
- **16/16 反例转绿**（原始目录+正式迁入双跑）；一轮 7/7、二轮 13/13 复跑保持；**598 前端（78 文件）+ 44 Rust 测试全过**；typecheck/lint 0；新产物门禁 PASS（1009.81 KiB，missing: []）。
- **R3-01 历史组身份**：pending 带稳定 groupId+isNewGroup 元信息（同组后续事务不覆盖）；物化钩子按注册身份条目（共享函数不再被误删）；物化失败保留 pending 并中止导航。
- **R3-02 source 首开**：Markdown 能力注册独立于 visual 实例（挂载即注册）；saveDocument 无实例快照时先物化 pending；补 3 例宿主测试。
- **R3-03 会话身份**：pending 捕获会话代际（物化前校验）；代际表改全局单调 token+活动会话表（可清理，documents 重建无条件换 token——C08 根因之一）；新增 3 例清理测试。
- **R3-04 另存为**：同窗口冲突前端自查（Rust 无 already-open(self) 分支）；finally 按新取得授权释放；会话身份在任何 await 前捕获并逐点校验；注销期间输入经 remove 前重读保留。
- **R3-05 关闭事务**：closing 状态（beginClosing 停止接纳——代际条件判定，重开自动恢复）；公开 disposeTabLifecycleAsync 完整四步；代际条件注销。
- **R3-06 Drawio**：导出超时返回 null（不回退旧镜像）；请求 requestId 匹配（迟到回包不写正文）；消息订阅稳定（不随内容更新重建）。
- **R3-07 回收**：suspend 返回后重新校验注册表 instanceId/迁移/IME；AppShell 渲染集合按曾挂载集合管理（属性更新不重挂载已回收标签——C15）；收敛批次统一（previous 同为候选）。
- **R3-08 迁移**：目标 unknown 保留写保护（与源对称）；Rust prepare 先处理不可逆终态再做 revision 对账（已提交不可被迟到错误 revision 回退）+ 新 Rust 测试。
- **R3-10 外部修改**：重新加载/覆盖接真实编排（读盘应用/受保护保存，失败保持冲突）；recheck 按代际条件提交、路径规范化匹配、modified 不再跳过复核。
- **R3-11 门禁/诊断**：静态闭包识别父级相对导入；模块清单缺条目/格式错误失败关闭（两夹具 actualExit=1）；perfMarks 上报单运行者；就绪标记改匿名会话序号（不带路径）。
- **仍未完成（报告"仍未完成项"逐条列出）**：prepare reservationId 深化、modified 写前屏障、requestId 路径归一、保存结构化返回、逐类型真机等价验证（真机/安装版运行类须用户现场执行）。
- **Release 已按四轮最终源码重新打包**（2026-09-07）：noteboard.exe（20,515,840 B，SHA-256 7fa6f1d8…670a）与 NoteBoard_0.2.7_x64-setup.exe（7,806,315 B，SHA-256 e0c36a6a…d1de）——三轮旧指纹作废；构建后全量验证复跑通过。
- 未 commit/push（等待用户审阅）；无残留进程。

---

## R4 整改记录（2026-09-07 第六轮）

依据《四轮复审与体验恢复方案.md》（7 个行为反例 D02–D08 + 2 对照 + "小文件持续加载"验收阻塞项）完成全部程序修复，详细变更见 `docs/performance/R4整改完成报告.md`。反例已迁入 `test/performance/round4/`（行为断言原样保留，仅类型断言/未用 import 两处接线适配）。

**核心结论：**
- **9/9 专项转绿**（原始目录+正式迁入双跑）；一轮 7/7（retry 用例 mock 补齐新导出）、二轮 13/13、三轮 16/16 复跑保持；**608 前端（83 文件）+ 44 Rust** 全过；typecheck/lint 0；门禁 PASS + 两对抗夹具正确拒绝。
- **R4-01/D03 激活与回收分离**：收敛批次先确定驱逐对象（候选中除 previous 外），保留实例（previous=刚切走标签）经新能力 `hasUnconfirmedInput()` 判定零未确认输入时跳过全文 flush——两个热标签往返零序列化；批次调度身份（旧批次迟到提交不改写渲染集合）。
- **R4-02/D08 模块资源注册表**：editorLoaders 模块级维护 idle/loading/ready/error——缓存 loader Promise/成功组件/稳定 lazy 包装，成功后跨宿主复用（重挂载不再进 Suspense）；仅失败重试重建包装；prefetch 与渲染共享条目。旧"每次新实例"实现形态断言改为行为断言（成功复用/失败可重试）。
- **R4-03/D02 resolvedMode 单次决策**：大文档+visual 标签不再被 initialMode 覆盖回 visual——统一 resolvedMode 应用一次，必有可用内核。
- **R4-05/D04/D05 closing 会话化**：beginClosing 总是记录当前代际（第二次关闭保护）；事务完成释放 closing 条目+活动表回落（30 路径关闭零泄漏）；disposeTabLifecycleAsync 独立入口补齐会话终结。
- **R4-06/D06 另存为交接**：remove 前显式物化 J2 pending（source/visual）——注销等待期间的输入进入新会话最终内容。
- **R4-07/D07 Drawio 回包归属**：协议核对（export 回包无请求 ID）——按"每请求恰一回包+串行顺序"实现作废名额计数：超时请求登记待收回包名额，迟到回包先消耗名额（A 超时后 B 在途不接纳 A 的内容）。
- **小文件持续加载定位闭环（验收阻塞项）**：隔离真实浏览器（真实 AppShell/Markdown 全扩展/StrictMode+内存 IPC+41 字符短 MD）快速往返 24 次后停在 A——停止 8 秒零可见加载提示、正文全程正确、无错误；诊断字段落地（getEditorResourceStatus/getEditorLoadDiagnostics+fallback 提交计数，匿名日志）；真机 dev/Release 复现受数据隔离约束未执行（保持开放，用户现场按报告内"真机诊断指引"采集）。
- **"两实例"硬指标撤销的执行**：默认回收策略原样保留（活动+最近一个）未擅自改动；缓存策略候选（工作集/闲置/低内存模式）待用户确认。
- **Release 已按 R4 源码重新打包**（2026-09-07）：noteboard.exe（20,515,840 B，SHA-256 62f8f110…2555）与 NoteBoard_0.2.7_x64-setup.exe（7,803,567 B，SHA-256 334e5714…9744）——四轮报告指纹作废；构建后全量验证复跑通过。
- **仍未完成（R4 报告"仍未完成项"逐条列出）**：真机绝对值验证、缓存策略用户确认、R4-04 首开 Markdown 闭包拆分（2102.62 KiB 静态 JS）、全链路分项计数、前三轮遗留项。
- 未 commit/push（等待用户审阅）；本轮启动的 Vite/Chrome 均已终止清理（用户既有进程未动）。

---

## P0 修复记录（2026-09-07 第七轮）

依据用户真机反馈（三个小文件内存升至 4GB；新建 MD 须切走再切回才能打开）完成定位与修复，详细变更见 `docs/performance/P0内存与新建文档修复报告.md`。

**核心结论：**
- **P0-2 内存泄漏根因（实证定位）**：VisualKernel 传入 LinkClickHandler options 的组件闭包经 **V8 Context 共享**（与捕获 editor 的 useCallback 共享组件作用域）持有完整 editor 圈，而该 options 处于 schema→NodeType 链上（被 prosemirror 模块级 ResolveCache 强持有）——**每次切换/回收重建泄漏一个完整 editor+schema+扩展圈**。定位链：WeakRef 存活探测 + 主动 GC 的 A-J 十组对照实验（AppShell→TipTapEditor→VisualKernel→options→闭包逐层二分）+ 42MB heap snapshot 反向解析保留路径。
- **修复**：`onOpenLinkModal` 改模块级工厂 `makeLinkModalOpener(docKey)`（闭包只捕获参数作用域）+ `emit('open-link-modal')` 解耦（组件内已有监听）；`openLinkModalRef` 桥接删除；源码注释写明禁令（禁止向 extension options 传组件内闭包）。
- **修复验证**：120 次切换被驱逐 editor 存活 240/240→close-all 后 0/240；heap 线性 +18.1MB→趋平 +6.6MB（增速递减）；VisualKernel 挂卸 60/60→0/60。
- **P0-1 新建 MD**：协调器级/AppShell 级/隔离真实浏览器（真实扩展+StrictMode+真实 newMarkdown）三层复现均正常（1.8s 可编辑，无需切换）——判定为内存泄漏的次生症状（4GB 下 GC 压力拖慢加载）；防御性补齐新建路径 `prefetchEditor`（与打开路径一致）。新增正式测试 2 文件 3 例（newDocumentFirstRender/newDocumentAppShell）。
- **全量验证**：611 前端（85 文件）+ 四轮专项 9/9×2 + 44 Rust + typecheck/lint 0 + 门禁 PASS。
- **Release 已按 P0 修复源码重新打包**（2026-09-07）：noteboard.exe（20,515,840 B，SHA-256 a324d17a…158e）与 NoteBoard_0.2.7_x64-setup.exe（7,748,828 B，SHA-256 a469c57b…3883）——R4 报告指纹作废；**建议用户优先用新安装包复测**（三文件频繁切换观察内存曲线 + 新建 MD 首次显示）。
- **仍未完成**：真机复测（P0-1 自愈判定 + P0-2 内存曲线确认，须用户现场绑定新指纹）、残余趋平增长分项计量、后续扩展回调的模块级工厂规范；前轮遗留项不变。
- 未 commit/push（等待用户审阅）；本轮启动的 Vite/CDP Chrome 均已终止清理（用户既有进程未动）。

---

## P0-1b 追加修复记录（2026-09-07 第八轮）

用户复测反馈：新建 MD 仍一直显示"正在加载编辑器"（切走再切回才好）；1.8s 太慢要求 1s 内。详细变更见 `docs/performance/P0内存与新建文档修复报告.md` 的"P0-1 追加修复"段。

**核心结论：**
- **机制级根因（测试实证）**：EditorHost 经 React.lazy 渲染——ctor 即便返回已 resolve 的 promise，首次渲染仍必 suspend、fallback 必提交一次（pending→微任务→retry 链）；慢环境下该 fallback 期被放大为"一直显示"。零 fallback 断言复现（ready 后挂载 fallback 计数 +2）。
- **修复三层**：(1) EditorHost 弃用 lazy/Suspense——useSyncExternalStore 订阅资源状态，ready 同步渲染缓存组件（零 fallback）；loading 占位；error 错误视图+安全重试（retryEditorLoad 重建并通知订阅者）。(2) WelcomeScreen 首帧后空闲预取 markdown+code（requestIdleCallback，jsdom 回退 setTimeout）。(3) editorSuspension 的 capabilities=null 分支 keep 命中未登记 keepAliveKey——加载中的标签被误逐出渲染集合（切回重挂载再显 fallback）。
- **验证**：隔离浏览器 wait/fast 两场景均 **100ms 可编辑**（目标 <1s，余量 10 倍）、fallback 自动退出、零错误；正式测试新增"预取后新建零 fallback"断言（263ms）；一轮 retry 反例按新接口适配（行为断言不变）。
- **全量**：612 前端（86 文件）+ 一~四轮反例 7/13/16/9 全绿 + 44 Rust + typecheck/lint 0 + 门禁 PASS。
- **Release 重打包**（2026-09-08）：noteboard.exe（20,515,840 B，SHA-256 e5581032…f88e）与 NoteBoard_0.2.7_x64-setup.exe（7,805,377 B，SHA-256 6b68d192…09b5）——旧指纹作废。
- 未 commit/push（等待用户审阅）；本轮启动的 Vite/Chrome 均已清理。
