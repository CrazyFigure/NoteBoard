# 三轮复审意见与整改清单

复审日期：2026-09-06。对象：当前未提交工作区、《二轮整改完成报告.md》《三轮收尾完成报告.md》及现有 Release 产物。本文是新的复查任务书，不覆盖前两轮意见、交付报告或实施记录。本轮没有修改业务代码，没有 commit/push，没有启动 NoteBoard、安装程序或开发服务器。

**结论：不能按“除真机验证外全部完成”验收。前两轮具体反例已修，但新增历史合并、回收和事务路径仍有内容覆盖、历史丢失、所有权失效及内存回收失效问题。**

本轮独立复跑：前两轮反例 **7/7、13/13 通过**；正式前端 **576/576**、Rust **43/43**、typecheck、lint 通过；现有产物闭包 **1006.486328125 KiB**，现有门禁通过；旧 mermaid 夹具正确以 `module-source:mermaid` 拒绝；两个 Release 文件哈希与报告一致。上述进展保留。

但本轮新增 **16 个行为反例全部失败**，另有 **2 个新门禁对抗夹具错误放行**。失败不是测试模块无法加载，而是真实编排/快照/宿主调用后的行为断言失败。尤其：第二个历史组连续输入两次就会丢组；Drawio 导出超时仍批准回收；同窗口另存为仍覆盖已打开目标；source 首开立即保存仍可能保存旧镜像；四标签切换后保留三个实例，修改脏态后全部四个实例重新挂载。

以下 P1 表示需先修复的正文、历史、生命周期或主要功能问题；P2 表示性能工具和测量等问题。每项都写明触发条件，不表示所有操作必现。代码确认但未运行组合反例的发现，另行标明。

## 复跑结果与证据

证据目录：[review-20260906-round3](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3)。该目录被 Git 忽略，交给其他模型时须一并提供，或迁入正式测试。原有两轮反例与本轮反例不可相互替代。

关键受审源码、HEAD 与 Release 哈希另存于 [fingerprints.json](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/fingerprints.json)，便于区分本次审查对象与后续修改。

| 验证 | 本轮结果 | 原始证据/范围 |
| --- | --- | --- |
| 一轮反例 | 7 通过，exit 0 | `round1-rerun.json` |
| 二轮反例 | 13 通过，exit 0 | `round2-rerun.json` |
| 正式前端测试 | 576 通过，exit 0 | `unit-results.json` |
| 本轮反例 | 16 失败，exit 1 | [results.json](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/results.json)；C01 分 visual/source 两例，C02–C15 各一例 |
| Rust 测试 | 43 通过，exit 0 | 本轮实际运行 `cargo test --manifest-path src-tauri/Cargo.toml`；有提示性 linker warning |
| 类型检查、lint | 通过 | 本轮实际运行 `pnpm typecheck`、`pnpm lint` |
| 当前 dist 闭包 | 2 个 JS，1006.486328125 KiB，无缺文件 | `actual-closure.json`；入口 `index-DrNKltEG.js` |
| 现有门禁及旧夹具 | PASS；夹具 actualExit=1 | `gate-fixture-result.json`，检出方式 `module-source:mermaid` |
| 新门禁对抗夹具 | 两例 actualExit=0，均应为 1 | [gate-adversarial-results.json](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/gate-adversarial-results.json) |
| Release 指纹 | 与交付报告吻合 | 仅哈希核验，本轮未安装、运行或重新构建 Release |
| 真机 IME/原生多窗口/性能采样 | 未运行 | 不计作已验证，也不据此报告真实启动和内存数值 |

Release SHA-256 核验结果：

- `noteboard.exe`：`6b3855a78873244f7674b311df712ebe602e654a409bce58be09e9bce51ae883`。
- `NoteBoard_0.2.7_x64-setup.exe`：`5ec74396a87566718990f208e8fb383fa47795fa34a3b0ea32513ca4f7b9b849`。

本轮使用真实 history、store、保存/关闭/迁移编排、AppShell 和 Drawio 组件；Tauri 文件 I/O、窗口事件、对话框、远程 iframe 消息及无关 UI 使用替身。C14 控制 source 首开“尚无 visual 能力”的真实前置状态，未声称完整挂载真实 Markdown 双内核；C15 使用真实 AppShell 和回收调度器，编辑器本体为可回收替身。

运行命令（项目根目录）：

```powershell
pnpm exec vitest run --config test-results/performance/review-20260906-round3/vitest.review.config.ts --reporter=json --outputFile=test-results/performance/review-20260906-round3/results-after.json
pnpm exec vitest run --config test-results/performance/review-20260906-round2/vitest.review.config.ts
pnpm exec vitest run --config test-results/performance/review-20260906/vitest.review.config.ts
node test-results/performance/review-20260906-round3/check-gate-adversarial.mjs
```

第一条当前应失败，整改后应转绿。最后一条需读取两个夹具的 `actualExit`：正确门禁应各为 1，不能只检查外层脚本正常结束。夹具只在独立目录复制被测脚本，不修改真实 dist。

## R3-01 · P1：J2 同组合并会丢掉“本组新建”标记；共享钩子会被其他实例注销

位置：[visualSnapshot.ts:53](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/visualSnapshot.ts:53)、[第 146 行](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/visualSnapshot.ts:146)、[documentHistory.ts:296](C:/Software/WorkSpace/NoteBoard/src/features/history/documentHistory.ts:296)。反例：[history.test.ts](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/history.test.ts)。

**C01 两种模式均复现：**已有 `base → group-one`，第二组第一笔暂存 `isNewGroup=true`，第二笔同组暂存 `false`。暂存函数展开新 snapshot，覆盖掉第一笔的 true；物化以 false 调用 recordDocumentChange，第二组被合入第一组。撤销实际回到 base，正确应回到 group-one。

正式 `visualHotPath.test.tsx` 的第二组只有一笔输入，所以不会触发此问题；第一组多笔也因 history.index=0 特殊处理而通过。因此“多次输入 + 跨组”这几个词出现在测试标题中，并不代表组合已覆盖。

**C02 已复现：**两个 Markdown 实例注册同一个 `flushPendingSourceSnapshot` 函数，Set 只存一份；任意一个实例卸载时 delete 掉它，仍挂载实例失去导航前物化屏障。此时读当前历史仍返回 base，pending-edit 未进入历史。visual 也使用同一共享函数注册方式。

整改要求：

1. 暂存组必须有稳定 groupId 和“相对已提交历史是否新组”的元信息；同组后续事务只替换组末 doc/Text、revision、末端选区，不得覆盖组起点和新组属性。物化后同一原生组继续输入的情况单独定义。
2. 钩子按会话/实例注册，或对共享函数做正确引用计数；返回的 disposer 只能释放自己的注册。不要让所有实例共享一份可被任意组件删除的 Set 项。
3. 序列化异常时不要先丢 pending 再继续 undo/redo。当前两个 flush 都先 delete，history 钩子又 catch 后继续导航；应保留待物化快照并中止依赖它的导航/保存，给出可重试结果。
4. 审核“每键 O(1)”口径：VisualKernel 第 101 行仍对每次事务调用 `findDiffStart`，新组边界还同步物化上一组。先证明真实热路径成本，再声明复杂度；不要以序列化调用次数替代耗时/长任务证据。

验收：至少三组、每组多笔；500ms 物化前后继续同组；每一组的撤销/重做和选区都保留；两个及以上实例并存、关闭/回收其中一个后其余仍可立即 undo/切模式/保存；序列化失败不丢 pending。

## R3-02 · P1：Markdown source 首开没有能力注册，立即保存绕过 pending

位置：[TipTapEditor.tsx:217](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/TipTapEditor.tsx:217)、[source 初始化段](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/TipTapEditor.tsx:502)、[saveDocument.ts:63](C:/Software/WorkSpace/NoteBoard/src/features/editor-code/orchestration/saveDocument.ts:63)。反例 C14 位于 [save.test.ts](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/save.test.ts)。

能力注册 effect 被 `if (editor)` 包住，editor 是 visual TipTap 实例。按 source 首开只创建 CodeMirror，visual 未挂载，注册表没有 Markdown capability。新 J2 将 source 内容放进 pending，store 在 500ms 前仍是旧值；此时 Ctrl+S 的 flushDocument 返回 null，保存函数回退旧 store 内容并返回成功。C14 实际写入 base，期望 source-new-input。

这不是只能真机验证的缺口。组件注册条件和保存链已经明确不匹配，也影响首次 source 的工具栏、搜索、迁移、回收与 ready 标记。

整改：能力对象由 Markdown 会话协调器注册，独立于 visual 是否存在；按当前模式取真实内核。source、visual 都必须在内容出口前完成对应 pending 屏障。明确区分“没有实例但已有可靠镜像”和“实例/快照未就绪或失败”，不能统一返回 null 后回退旧正文。

验收：用户默认 source、大文件 source 首开、从未进入 visual 的文档，输入后立即保存/暂存/迁移/关闭/切模式；磁盘与历史含最后输入，且不为修复此问题提前创建 TipTap。补真实 TipTapEditor source 宿主测试。

## R3-03 · P1：新 pending 与重型 adapter 绕过已建立的会话屏障

位置：[visualSnapshot.ts:82](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/visualSnapshot.ts:82)、[第 164 行](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/visualSnapshot.ts:164)、[TipTapEditor.tsx:665](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/TipTapEditor.tsx:665)、[documentSession.ts:126](C:/Software/WorkSpace/NoteBoard/src/features/session/documentSession.ts:126)。

**C03 已复现：**旧 source pending 尚未物化 → 关闭旧会话、同路径重开 → 旧 pending 物化。新正文从 new-session 被覆盖成 old-pending。

原因是 pending 只用 docKey 索引，没有 session/instance 身份；物化直接 recordDocumentChange/setContent，绕过 submitCapturedContent。revision 虽被捕获却没有参与校验。Markdown 卸载 cleanup 还会无条件物化两个模式/写镜像，然后按路径 discard，可能影响同路径新会话。Board cleanup 也直接按 key 写回。

新 Mindmap/Bitable/Drawio 及现有 Board adapter 调用 submitCapturedContent 时没有传 sessionGeneration，而该参数仍是可选的。flushDocument 的外层检查发生在 adapter 已产生副作用之后，不能追回被覆盖的数据；保存、迁移等调用方还存在失败快照回退镜像的路径。

整改要求：

1. 会话身份成为捕获和提交的必填契约，覆盖 pending、定时器、history hook、adapter、回收、读盘、卸载和另存为，不允许新模块另开直写入口。
2. 在任何正文/历史副作用之前校验 session、instance、revision 和当前权威模式；失效结果明确返回，不能先写 store 再让外层拒绝。
3. 关闭/丢弃/迁移按具体会话释放 pending；旧 cleanup 不能物化或 discard 新会话的内容。React cleanup 只释放已经交接的资源，不承担未完成正文交付。
4. `sessionGenerations` 当前对每个访问过的路径永久保留条目。改用全局单调 token 加活动会话表等可清理设计，保证清理后旧 token 仍不能误碰新会话，避免长期运行路径表增长。

验收：C03 转绿；补 visual/source/Board/Drawio 的旧回调与同路径新会话竞态，断言正文和历史均不被改动；关闭大量不同文件后，相关 Map 与监听计数能回落。

## R3-04 · P1：另存为事务仍缺同窗口冲突、回滚和完整身份保护

位置：[saveDocument.ts:228](C:/Software/WorkSpace/NoteBoard/src/features/editor-code/orchestration/saveDocument.ts:228)、[第 271 行](C:/Software/WorkSpace/NoteBoard/src/features/editor-code/orchestration/saveDocument.ts:271)、[第 285 行](C:/Software/WorkSpace/NoteBoard/src/features/editor-code/orchestration/saveDocument.ts:285)。后端语义：[documents.rs:37](C:/Software/WorkSpace/NoteBoard/src-tauri/src/registry/documents.rs:37)。反例 C04–C07 已复现。

| 反例 | 触发 | 实际结果 |
| --- | --- | --- |
| C04 | 目标已在本窗口打开并有正文；register 返回后端真实的 ok | saveAs 返回 true、写盘一次，目标正文被源内容覆盖；可能形成重复 key 标签 |
| C05 | 成功注册新目标，但 writeDocument 失败 | 返回 false，却没有注销本次新取得的目标归属 |
| C06 | 保存对话框等待时源关闭并同路径重开 | 旧 saveAs 迁走新会话并写目标，原新会话被删除 |
| C07 | 捕获 finalContent 后，等待注销旧路径期间又输入 | 新目标仍是 before，typed-during-unregister 被丢失 |

报告声称同窗口已有目标会返回 already-open(self)，但 Rust 同 owner 的幂等 register 实际返回 Ok；前端也没有独立检查目标标签。这属于 IPC 契约与测试替身不一致，不能通过改测试让 Rust 返回并不存在的分支。

整改要求：

1. 目标预留接口区分“此次新取得”“同会话幂等”“同窗口另一会话”“其他窗口占用”，并返回可校验的 reservation/owner token；检查规范化路径身份。来源会话从调用开始捕获身份，每个 await 后验证。
2. 未提交成功的目标授权在 finally 中按 token 释放；只释放本次取得的授权，不能误注销原已存在的目标。快照失败、首写失败、迁移失败均覆盖。
3. 建立一个短且明确的身份切换临界区：停止旧身份接纳写任务、排空允许完成的任务、捕获最终状态、切换会话和标签；用户在等待中产生的新输入必须进入新的权威会话。当前“先捕获，再 await drain/unregister，再 remove”仍留有输入空窗。
4. 保存结果返回 `{ outcome, sessionId, key, savedRevision }` 等明确结果。删除全局单槽 lastSaveIdentityMove；并发保存/失败重试不能消费别的操作或上一次操作的身份迁移结果。
5. 新 key 的内容、历史、基线、dirty、保存策略、暂存引用及 pendingCloseKeys 一次性对齐；不得为通过测试只补目标名称或只标 dirty。

验收：C04–C07；同窗口同名不同大小写路径、目标占用、授权/写入失败、两次同时另存、对话框期间关闭重开、排队/注销期间继续输入、未命名保存并关闭。以最终正文、磁盘调用、owner 和标签唯一性作为断言。

## R3-05 · P1：公开异步关闭接口没有停止接纳，旧注销也没有代际

位置：[windowStore.ts:117](C:/Software/WorkSpace/NoteBoard/src/stores/windowStore.ts:117)、[第 132 行](C:/Software/WorkSpace/NoteBoard/src/stores/windowStore.ts:132)、[windowManager.ts:448](C:/Software/WorkSpace/NoteBoard/src/features/window/windowManager.ts:448)。反例 C08/C09 已复现。

停止接纳和 dispose 在私有同步 disposeTabLifecycle 中，公开 `disposeTabLifecycleAsync` 只有 drain→unregister。performWindowClose 直接调用后者，未经过停止接纳。C09 await 该函数后再次提交旧会话写入仍成功，与报告“统一异步接口完整执行停止接纳→排空→注销”不符。

C08 在旧关闭等待写队列期间重开同路径并注册新归属；旧 drain 完成后仍发出 unregister。后端只校验窗口 label，无法区分本窗口的新旧会话，新归属会被旧关闭注销。

整改：一个真正统一、按 session/owner token 幂等的异步关闭事务，由普通标签关闭、批量关闭、窗口整体关闭调用；在第一次 await 前进入 closing 并停止接纳，按关闭原因处理未保存内容，再等待在途 I/O，最后按 token 条件注销。转移清理使用 transferred 原因。旧关闭晚到不应通过重新读取“当前 generation”获得注销新会话的权限。

验收：C08/C09；窗口等待关闭 IPC 期间继续到达的定时保存、旧 I/O、旧 editor cleanup 不产生越权写；关闭失败能安全恢复明确状态；旧关闭不能移除新会话的 Rust 归属。

## R3-06 · P1：Drawio 没有满足“有效导出回包后才回收”

位置：[DrawioEditor.tsx:72](C:/Software/WorkSpace/NoteBoard/src/features/drawio/DrawioEditor.tsx:72)、[第 102 行](C:/Software/WorkSpace/NoteBoard/src/features/drawio/DrawioEditor.tsx:102)、[第 216 行](C:/Software/WorkSpace/NoteBoard/src/features/drawio/DrawioEditor.tsx:216)。反例：[lifecycle.test.tsx](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/lifecycle.test.tsx)。

**C10 已复现：**引擎 init 后请求 XML 导出，不返回回包，3 秒超时。flush 回退非空旧镜像，suspendEditorInstance 仍返回 true。没有最新内容证明却允许卸载，直接违反前两轮要求。

**C11 已复现：**导出请求超时 → 后来 autosave 已交付 new-edit → 旧 export 回包到达。即使没有 pending，事件处理器仍无条件 setContent(old-export)，覆盖新正文。串行槽加超时不构成请求匹配：超时后的旧回包还可能匹配后一次请求。

其他代码问题：message effect 依赖 doc.content，每次内容更新都会 cleanup 并终结正在等待的导出；close 分支直接把最近 autosave 镜像称为权威；XML 回包提交未携带 session/revision 的捕获证明。

整改：使用真正的请求标识和引擎/会话代际关联，或在无法保证对应关系时保持不可回收。超时/并发未取得快照必须返回失败，不能回退旧镜像当成功。迟到/无匹配回包不写正文；订阅随 iframe 生命周期保持稳定。关闭同样必须先确认最新内容已交付，不能以“iframe 即将销毁”为理由跳过屏障。

验收：C10/C11；请求 A 超时后 B 发起，再到达 A/B 回包；导出期间 autosave；引擎重载；网络失败；真实未保存编辑的回收/恢复。保守保留实例是暂时可接受的状态，不能把未能安全导出的类型标成 S12 完成。

## R3-07 · P1：回收缺少最终身份校验，渲染集合会重新挂载已回收实例

位置：[editorSuspension.ts:57](C:/Software/WorkSpace/NoteBoard/src/features/session/editorSuspension.ts:57)、[AppShell.tsx:236](C:/Software/WorkSpace/NoteBoard/src/components/AppShell.tsx:236)、[第 297 行](C:/Software/WorkSpace/NoteBoard/src/components/AppShell.tsx:297)。反例 C12/C15 已复现。

C12：旧实例 flush 挂起，新实例注册接管，旧 flush 返回。调度器只从旧 capabilities 对象读取 revision，不重新验证注册表 instance/session，也不再次校验 canSuspend/迁移状态，仍批准旧回收。

C15 使用真实 AppShell 与真实调度器，四个均可回收标签依次打开：切换完仍挂载 **3** 个；仅 setTabDirty 当前标签后变成 **4** 个，预期均为活动+最近一个，共 2 个。

原因包括：逐候选 suspend 会反复改 keepAliveKey，最后一个候选被保留，previous 又不在 removable 中；更严重的是 `[tabs]` effect 每次把全部非 lazy 标签重新加入 mountedEditorKeys，dirty、viewMode 等标签属性更新也会触发它。输入中的脏态更新因而撤销了回收结果。

整改：把“文档存在”“实例已挂载”“已批准回收”“缓存保留选择”分成稳定状态。新增标签与普通属性变化分开处理，只有真正首次打开或用户激活才创建实例。一次回收批次确定保留对象，不能在逐项验证时不断更改缓存策略；提交卸载前校验 session、instance、revision、当前激活/IME/手势/迁移状态。已知不能恢复的实例继续明确 pinned。

验收：C12/C15；输入、切模式、改脏态不会重新挂载后台所有编辑器；快速 A→B→C→A、异步导出期间再激活、Home 往返；统计真实 mount/unmount、核心实例、监听及定时器数量，再进行 20 标签和 30 轮真机内存曲线。不能只断言 takeViewState 已被取走。

## R3-08 · P1：迁移目标在未知状态下仍解锁；Rust 终态校验顺序有倒退风险

位置：[windowManager.ts:209](C:/Software/WorkSpace/NoteBoard/src/features/window/windowManager.ts:209)、[commands.rs:239](C:/Software/WorkSpace/NoteBoard/src-tauri/src/window/commands.rs:239)。反例 C13 已复现。

目标 prepare 响应失败、query 也失败时，adoptTransferredDocument 的 unknown 分支仍 `exitTransfer(doc.key)`。C13 经真实目标启动/接纳链验证，正文保留但迁移保护为 false。只改源窗口的 unknown 处理，不能保证双方都受保护；报告对此描述过度。

另一个**代码已确认、尚未新增 Rust 反例**的问题：prepare_transfer_inner 先比 actual_revision，发现不同便写 Aborted，之后才 match 当前 state。已经 Committed 的事务收到不同 revision 的重复确认，可以被改回 Aborted，而 owner 已经切给目标。终态必须先处理并保持不可逆。

`actual_revision` 当前只是目标回传 payload 中原值，也没有验证源在捕获后是否又产生编辑/写盘；字段对账不能替代源会话冻结与实际写入授权。源 enterTransfer/blur 仍不是所有程序命令和 autosave 的共同写屏障。

整改：双方共用 committed/aborted/unknown 的终态处理器；unknown 保留正文与写保护，对账可重试；仅权威终态解锁/清理。Rust 先处理不可逆终态，再验证未提交阶段的版本；commit 和 abort 只能有一个终态。待迁移期间保存、历史命令、回收、关闭、IME 提交都纳入统一会话约束。

验收：C13；新增 Rust committed 后携带错误 revision 再 prepare 的测试；目标/源 commit 响应丢失、查询失败、旧事件迟到、对账完成前后输入。所有权和写权限始终一致。

## R3-09 · P1：prepare 仍不是完整的可兑现预约，失败分支继续创建未授权会话

代码确认，尚未新增真实 Rust/frontend 组合反例。位置：[prepare.rs:47](C:/Software/WorkSpace/NoteBoard/src-tauri/src/fsio/prepare.rs:47)、[registry/commands.rs:12](C:/Software/WorkSpace/NoteBoard/src-tauri/src/registry/commands.rs:12)、[openDocument.ts:113](C:/Software/WorkSpace/NoteBoard/src/features/editor-code/orchestration/openDocument.ts:113)。

- PendingPrepare 增加 reserved/TTL，但仍没有 reservationId。register_document 直接查 documents，并不验证其他窗口的 pending 预约；同 label 的旧/新预约也不能区分。后端写文件入口仍未验证此预约/会话授权。
- 在途和已打开仍共用 AlreadyOpen。跨窗口分支只 focusWindow，不保证原准备成功、目标标签已建立或目标标签被激活；请求可能被提前 ACK 为 focused。
- text/image 分支注册 IPC 失败时 catch 后继续建可用标签；image 在注册前先 upsert，冲突返回后未清理临时文档。unsupported 分支不注册，而后端却为它保留等待注册的预约。

整改：预约带 token、状态和等待/取消结果，register 只能兑现匹配预约；准备失败、窗口销毁、取消时按代际释放。错误不伪装为 opened/focused；成功聚焦必须落实到目标 key。不能继续用 TTL 或 `5×60ms` 重试代替这条协议，尤其用户要求的是快速双击打开。

验收：两窗口并发准备、准备未完时另存为同目标、旧读盘结束遇到新预约、注册失败、unsupported、目标最小化且打开的是非活动标签；无幽灵 owner、无双写、无成功 ACK 却未交付文档。

## R3-10 · P1：外部修改新增核对后，用户处理链和异步校验仍不完整

代码确认，未在本轮模拟真实外部写盘。位置：[directoryWatcher.ts:76](C:/Software/WorkSpace/NoteBoard/src/features/explorer/directoryWatcher.ts:76)、[第 98 行](C:/Software/WorkSpace/NoteBoard/src/features/explorer/directoryWatcher.ts:98)、[ExternalChangeBanner.tsx:19](C:/Software/WorkSpace/NoteBoard/src/features/editor-md/ExternalChangeBanner.tsx:19)。

核对现已会设置 modified，但不能因此认为整个冲突功能完成：

1. “重新加载”和“覆盖磁盘文件”两个按钮目前都只 setExternalStatus(clean)，没有读盘/应用内核/保存；tab 状态也未同步。用户点击重新加载后旧正文仍在，自动保存保护却解除。
2. watcher 事件路径直接作 Map key；与 Rust 规范化 key 的大小写、斜杠形式不同就找不到文档。只订阅 Explorer root 也不能覆盖其他仍打开文档所在目录。
3. recheckDocument 在 await readDocument 前捕获 doc 和 baseline，返回后无 session、baseline/write revision 校验；旧读取可误标新会话或与期间的自身保存竞争。已 modified/deleted 的路径又在调度入口被跳过，“外部写回基线恢复 clean”的正常事件链与报告不一致。
4. 600ms/1500ms 延迟期间仅安排将来读取，尚未阻止可能覆盖外部内容的旧 autosave。测试若先等核对完成再测试 autosave，会漏掉这个窗口。正文核对去抖也不代表目录 readDir 已合并。

整改：建立 pending-external-check 状态及写前核对屏障，准确处理自身事务；按规范化会话身份登记受影响文件，异步结果按代际/版本条件提交。重新加载必须确认并应用磁盘内容、内核、基线与历史规则；覆盖必须实际执行受保护保存，失败不解除冲突。覆盖所有可写类型的用户处理入口。

验收：真实 watcher→冲突→按钮→内核/磁盘全链；等长替换、保存后 100ms 外部修改且下一次 autosave 在复核前到达、大小写路径差异、后台另一目录文档、旧读取迟到、新会话重开。

## R3-11 · P2：预算门禁仍有漏检，requestId 与终点仍不足以支持性能验收

**两个新增门禁夹具已复现：**[check-gate-adversarial.mjs](C:/Software/WorkSpace/NoteBoard/test-results/performance/review-20260906-round3/check-gate-adversarial.mjs)。

| 夹具 | 原因 | 实际/期望 |
| --- | --- | --- |
| parent-relative-import | `assets/sub/entry.js` import `../shared-util.js`，后者来源为 mermaid；脚本只识别 startsWith('./') | exit 0 / 应 exit 1 |
| missing-chunk-provenance | 静态依赖存在，但模块来源清单缺该 chunk 条目；`?.packages ?? []` 当无依赖放行 | exit 0 / 应 exit 1 |

整改：[check-budget.mjs:107](C:/Software/WorkSpace/NoteBoard/scripts/check-budget.mjs:107) 正确解析全部产物本地静态边；[第 147 行](C:/Software/WorkSpace/NoteBoard/scripts/check-budget.mjs:147) 对缺项、格式错误、清单与产物不一致失败关闭。用构建指纹绑定清单和 chunk，保持当前已生效的包来源与 JS/CSS 字节预算。当前 dist 通过不等于其真的有 mermaid 泄漏；新增夹具证明的是门禁仍能被不完整输入绕过。

性能诊断还需修改：

- requestId 按原始 request.path 登记，实例按规范化 docKey 消费，两者可能不匹配；同一路径重复请求会覆盖未消费标识，已打开标签 focus 不会重挂载，也不会产生本次 ready 终点。
- Markdown source 首开无 ready；image/diagram/infographic 等没有全部接入这一统一终点。Markdown visual 的标记在后续 parse/init effect 前，Drawio 在发送 load XML 前就记 ready，不能代表正文已经可交互。
- `markReported(count)` 对单次上报修复有效，但两个并发 report 可发送同一批，再分别把游标加 count，跳过期间的新事件。需要序号区间确认或单运行者上报，不能宣称“天然去重”。
- `docKey.slice(-40)` 对短路径仍是完整路径。记录匿名会话/request/type 即可，不用路径尾部作为隐私保证。

验收：两个新夹具拒绝；缺清单/缺条目/目录相对边/字节超限回归。每种关联类型、source/visual、focus 已打开、同路径重复双击，requestId 都对应实际内容与能力就绪；注入首笔输入并检查历史校准终点。然后才能用这些事件统计 p50/p95。

## 重型回收“等价验证”的补充要求

目前不能把 `heavyEditorRecycle.test.tsx` 的 5 例写成五类已完整等价验证：Markdown 用例只断言“未挂载时 capabilities=null”；Drawio 只测引擎未就绪和 close 镜像回退，没有验证有效导出；Mindmap 将 store 改成新内容后 flush，再比较返回值等于被 flush 改写的 store，不能证明新内容被保留；Bitable 只检查快照形态，没有重挂载后实际状态断言。

代码中也仍有需要核验的具体视图问题：Bitable 用 viewTabRefs 读取/恢复 scrollTop，该 ref 挂在视图标签项上，而非正文滚动容器；Markdown visual 使用 contenteditable 父元素的 scrollTop，实际滚动容器在 VisualKernel 外层，需要用真实滚动验证；Mindmap 只保存模式/缩放，未覆盖节点选中、折叠、视口及输入中状态；Board 还需验证文本编辑/IME 和完整选择、撤销语义。

先列出每类编辑器回收前必须保留的状态、权威来源和成功确认点，再逐项写“修改前已知值 → 回收 → 实际卸载 → 重建 → 观察实际恢复值”的测试。不能以快照字段存在、takeViewState 返回 null、序列化后自比较替代功能等价。尚未证明的类型保持不可回收，明确标为未完成，后续再通过可靠实现取得内存收益。

## 实施顺序与交付口径

1. 保留并迁入本轮 16 个反例、两个门禁夹具。行为断言不改成当前错误值；改 API 时可更新接线，但磁盘、正文、历史节点、所有权和真实实例数量仍是验收依据。
2. 先完成 R3-01/02/03：统一 session/instance/组身份、物化失败契约、Markdown 双模式能力注册。不要继续为新 adapter 增加无身份的直写通道。
3. 完成 R3-04/05/08/09 的授权和终态事务。Rust 与前端共享可校验身份；修复需覆盖失败、重复、迟到、关闭重开和跨窗口交错，不能只改一个 if。
4. 完成 R3-06/07 及逐类型等价验证，再开启重型回收。保留 Home 不卸载的有效修复，同时解决真正的缓存预算与脏态重新挂载问题。
5. 完成 R3-10/11 的完整处理链与测量工具，然后从最终源码重建 Release、重新记录哈希并做实际安装版验收。
6. 新交付报告逐条标记“反例已通过”“组合验证已通过”“真机已通过”与“未完成”。S12、S13/J2、统一关闭、prepare 预约、外部修改全链、全部编辑器终点当前均不能标成全部完成。

真机数据隔离是运行测试的约束，不是把可静态检查或可用内存替身复现的代码缺口移交用户的理由。先完成本文确定可实施的修复和测试；运行阶段准备可核验的隔离数据目录、测试文件和回滚方式，再按已授权范围执行，确实无法隔离的部分才明确留待现场。

最后仍需按原计划保留全部功能，测量同机记事本、最小 Tauri 宿主与实际安装版的分段启动、首个有效输入、20 标签进程组内存、关闭回落和 30 轮循环。**这次复查证实了产物存在、指纹一致及既有测试通过；没有证据证明“类似记事本的速度、低内存、极致丝滑”的最终目标已经达到。**
